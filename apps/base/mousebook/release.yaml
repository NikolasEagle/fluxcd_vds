apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mousebook
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: mousebook
      version: ">=1.0.0"
      sourceRef:
        kind: HelmRepository
        name: mousebook-helm-repo
        namespace: flux-system
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: mousebook-config
      valuesKey: values.yaml
