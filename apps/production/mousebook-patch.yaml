apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mousebook
  namespace: default
spec:
  valuesFrom:
    - kind: ConfigMap
      name: mousebook-config
      valuesKey: values.yaml
